<!DOCTYPE html>
<html lang="it-IT">
	<head>
		<% include ../partials/header.ejs %>
	</head>

	<body>
		<table class="table table-hover">
			<thead>
				<tr>
					<th scope="col">N°</th>
					<th scope="col">Title</th>
					<th scope="col">Year</th>
					<th scope="col">Genre</th>
					<th scope="col">Actors</th>
				</tr>
			</thead>
			<tbody>
				<% var i = 0; %>
				<% for(var x = 0; x < films.length; x++) { %>
				<tr>
					<th scope="row"><%= ++i %></th>
					<td><%= films[x].title %></td>
					<td><%= films[x].year %></td>
					<td><%= films[x].cast %></td>
					<td><%= films[x].genres %></td>
				</tr>
				<% if(x == 99) break; %>
				<% } %>
			</tbody>
		</table>
	</body>
</html>